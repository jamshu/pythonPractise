def integers(n):
    """Infinite sequence of integers."""
    i = 1
    while i<n:
        yield i
        i = i + 1

z= [(x, y, z) for z in integers(7) for y in range(1, z) for x in range(1, y) if z*z == x*x + y*y]
print 'jhjhjhhj',z
